# --- Theme & Colors (Autónomo / Standalone) ---
theme = TokyoNight
selection-background = #7aa2f7
selection-foreground = #1a1b26
split-divider-color = #7aa2f7

# Legibilidad de texto y archivos atenuados (.bak)
palette = 7=#b4bee6
palette = 8=#565f89

# --- Font ---
font-size = 12
font-style = Regular

# --- Window / Layout ---
window-theme = system
window-subtitle = working-directory
window-padding-x = 18
window-padding-y = 14
window-padding-balance = true
confirm-close-surface = false
resize-overlay = never
gtk-toolbar-style = flat
window-decoration = auto

# --- Look & Transparencia ---
background-opacity = 0.95

# --- Splits / Multiplexing ---
unfocused-split-opacity = 0.88

# --- Cursor styling (Bloque sólido relleno) ---
cursor-style = block
cursor-style-blink = false
cursor-opacity = 1

# --- Shell Integration & Features ---
shell-integration = detect
shell-integration-features = ssh-terminfo,ssh-env,no-cursor,sudo

# --- UX / Safety ---
scrollback-limit = 20000
clipboard-paste-protection = true
clipboard-paste-bracketed-safe = true

# --- Process / Lifecycle ---
quit-after-last-window-closed = false

# --- Keyboard bindings (CSI-u + Window splits) ---
keybind = shift+insert=paste_from_clipboard
keybind = control+insert=copy_to_clipboard
keybind = shift+enter=csi:13;2u
keybind = alt+shift+enter=csi:13;4u
keybind = super+control+shift+alt+arrow_down=resize_split:down,100
keybind = super+control+shift+alt+arrow_up=resize_split:up,100
keybind = super+control+shift+alt+arrow_left=resize_split:left,100
keybind = super+control+shift+alt+arrow_right=resize_split:right,100

# --- Calibrate mouse wheel scrolling speed ---
mouse-scroll-multiplier = precision:1.5,discrete:4
